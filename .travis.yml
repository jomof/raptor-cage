sudo: true
android:
  components:
    - tools
    - platform-tools
    - extra-google-m2repository
    - extra-android-m2repository
    
before_install:
  - chmod +x gradlew

script:
  - echo y | sdkmanager "ndk-bundle"
  - ./gradlew assemble
  - ./gradlew test --info
